---
# VagrantVM

rpi_deployer_ssh_prv: "vbox_id_rsa.vault"

# Common host configs ----------------------------------------------------------

host_dir: [
  { dir: '/opt/cluster/backup', mode: '0700', owner: '{{ ansible_user_id }}', group: '{{ ansible_user_id }}' },
  { dir: '/cluster/vbox', mode: '0770', owner: '{{ ansible_user_id }}', group: '{{ ansible_user_id }}' }
]

# SSH additional port
ssh_group_port: "1111"

#-------------------------------------------------------------------------------

# NTP server
# use this ntp.org pool:
use_ntp_pool: "au.pool.ntp.org"

#-------------------------------------------------------------------------------
