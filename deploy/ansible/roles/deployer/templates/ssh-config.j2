# {{ ansible_managed }}

host *
	ServerAliveInterval 30
	Compression yes
	controlmaster auto

host 192.168.6.*
	User pi

host *.local
	User pi

host {{ compute1_name }}.local
	HostName {{ compute1_ip }}

host {{ compute2_name }}.local
	HostName {{ compute2_ip }}

host {{ compute3_name }}.local
	HostName {{ compute3_ip }}

host {{ compute4_name }}.local
	HostName {{ compute4_ip }}
