---
# Varibles for LanServices main (Alpha and Beta nodes)

# Common host configs ----------------------------------------------------------

host_dir: [
  { dir: '/opt/cluster/lanservice', mode: '0750', owner: "{{ ansible_user_id }}", group: "{{ ansible_user_id }}" },
]

# extra SSH port for this group/host
ssh_group_port: "2220"

#-------------------------------------------------------------------------------

rpi_dnsd_enabled: true

# chrooted Busybox webserver
rpi_bb_httpd_enabled: true

# Gluster server:
rpi_glustersrv_enabled: false

#-------------------------------------------------------------------------------
