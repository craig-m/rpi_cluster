---
# Compute webapp container hosting


- name: gather facts on other hosts
  hosts: all
  gather_facts: True
  roles:
    - ansible-pre-role


- name: containers
  hosts: docker
  gather_facts: false
  roles:
    - docker
    - kubernetes


- name: gather facts on nodes
  hosts: all
  gather_facts: True
  roles:
    - ansible-post-role
