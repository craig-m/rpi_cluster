---
galaxy_info:
  role_name: group-compute
  author: crgm
  description: actions for compute nodes
  company: crgm inc
  license: license (no)
  min_ansible_version: 2.8
  platforms:
  - name: Raspbian
    versions:
      - stretch
allow_duplicates: no
dependencies:
  - { role: userac }
  - { role: base-system }
  - { role: ssh-server }
