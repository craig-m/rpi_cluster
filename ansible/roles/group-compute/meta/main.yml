---
galaxy_info:
  role_name: group-compute
  author: crgm
  description: sshd server config
  company: crgm inc
  license: license (no)
  min_ansible_version: 2.7
  platforms:
  - name: Raspbian
    versions:
      - stretch
allow_duplicates: no
dependencies:
  - { role: userac }
  - { role: base-system }
