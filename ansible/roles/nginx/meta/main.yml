---
galaxy_info:
  role_name: nginx
  author: crgm
  description: nginx webserver role
  company: crgm inc
  license: license (no)
  min_ansible_version: 2.8
  platforms:
  - name: Raspbian
    versions:
      - stretch
dependencies:
  - role: base-system
  - role: ufw
  - role: haproxy
