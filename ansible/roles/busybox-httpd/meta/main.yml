---
galaxy_info:
  role_name: busybox-httpd
  author: crgm
  description: a small webserver (busybox httpd) run in a chroot. 
  company: crgm inc
  license: license (no)
  min_ansible_version: 2.7
  platforms:
  - name: Raspbian
    versions:
      - stretch
dependencies:
  - role: base-system
