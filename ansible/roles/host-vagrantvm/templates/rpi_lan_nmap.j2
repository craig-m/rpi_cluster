#!/bin/bash
# {{ ansible_managed }}
# Created from vagrantvm role

portlist="20,21,22,25,53,68,69,80,88,110,111,123,443,647,953,1080,1337,2020,2220,2221,2222,3306,5353,6379,8080,8888"
scanoutml="/opt/cluster/data/lanscan.xml"

sudo echo "" > ${scanoutml}
sudo nmap -v -A -T4 -sS {{ rpi_net_id }}/{{ rpi_net_maskbit }} -p $portlist -oX ${scanoutml}

xsltproc ${scanoutml} -o ~/www/html/pub/"nmap_report_$(date +%m%d%y).html"
